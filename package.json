{
  "name": "penskillz",
  "version": "0.1.0",
  "private": true,
  "description": "Pentesting skill registry and extraction harness. Node dependencies are only used for husky-managed git hooks; the project itself is Python 3 + bash.",
  "scripts": {
    "prepare": "husky",
    "verify": "scripts/verify-staged.sh",
    "hooks:install": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=18"
  }
}
